<%= form_with url: orders_path,method: :post, data: {turbo: false}, local: true do |f| %>
  <div class="form-check">
    <%= f.collection_check_boxes :ingredient_id, @ingredients, :id, :title, class: "form-check-input" %>
      <%= f.submit class: "btn btn-success"  %>
  </div>
<% end %>
